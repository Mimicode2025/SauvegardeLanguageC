#include <stdio.h>
#include <conio.h>
void main()
{
    int nbre_page,prix;
    printf("veuillez saisir le nombre de page:");
    scanf("%d",&nbre_page);
    if (nbre_page<=10);
        prix=( nbre_page*5);
    else if ((nbre_page >= 11) || (nbre_page <= 20))
        prix= (10*5)+((nbre_p-10)*4);







}
